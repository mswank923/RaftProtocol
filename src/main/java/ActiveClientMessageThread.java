public class ActiveClientMessageThread extends Thread{

    private ClientNode node;

    public ActiveClientMessageThread(ClientNode node){
        this.node = node;
    }

    @Override
    public void run(){

    }
}
