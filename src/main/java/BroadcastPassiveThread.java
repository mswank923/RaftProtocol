/**
 * Thread that receives broadcasted addresses to recognize new or existing peers.
 */
public class BroadcastPassiveThread {

    private RaftNode node;

    public BroadcastPassiveThread(RaftNode node) {
        this.node = node;
    }

    @Override
    public void run() {

    }
}
